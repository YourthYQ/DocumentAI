# OpenAI API Key
# Obtain your API key from the OpenAI platform (platform.openai.com).
OPENAI_API_KEY="your_openai_api_key_here"

# Pinecone Configuration
# Obtain your API key and environment from the Pinecone console (app.pinecone.io).
PINECONE_API_KEY="your_pinecone_api_key_here"
# The Pinecone environment is crucial and depends on the cloud and region where your index is hosted.
# Examples: "gcp-starter" (for GCP starter environment), "us-east-1-aws", "us-west1-gcp", "europe-west1-gcp".
# You can find this value in your Pinecone console under your index details or API keys section.
PINECONE_ENVIRONMENT="your_pinecone_environment_here"
# Default Pinecone index name for the application.
# If this index does not exist, the application will attempt to create it at startup
# with the required dimensions (e.g., 1536 for OpenAI's text-embedding-ada-002).
PINECONE_INDEX_NAME="docuai-index"

# MongoDB Configuration
# These defaults are suitable for running MongoDB via the provided docker-compose.yml.
# If connecting to an external MongoDB instance (e.g., MongoDB Atlas, self-hosted),
# replace MONGO_URI with your full connection string and MONGO_DB_NAME with your database name.
MONGO_URI="mongodb://mongo:27017/docuai_db"
MONGO_DB_NAME="docuai_db"

# Redis Configuration
# These defaults are suitable for running Redis via the provided docker-compose.yml.
# If connecting to an external Redis instance, update REDIS_HOST and REDIS_PORT.
# A REDIS_PASSWORD might also be needed for external instances.
REDIS_HOST="redis"
REDIS_PORT="6379"
# REDIS_CONVERSATION_DB="0" # The default Redis database (0) is used for conversation history.
                          # This is typically configured directly in app/core/config.py if not overridden here.
                          # If you need a specific DB number and want to set it via env, uncomment and set.

# LLM Configuration
# Specifies the default model to be used for language generation tasks (e.g., answering questions).
# Examples: "gpt-4", "gpt-4-turbo-preview", "gpt-3.5-turbo".
# Ensure the chosen model is compatible with the OpenAI API key and your usage plan.
DEFAULT_LLM_MODEL="gpt-4"

# Application Settings (Optional Overrides)
# These settings have defaults in app/core/config.py but can be overridden via environment variables.
#
# CONVERSATION_TIMEOUT_SECONDS="3600" # Timeout for Redis session history (in seconds). Default is 3600 (1 hour).
# PROJECT_NAME="DocuAI"
# VERSION="0.1.0"
